name: Lint

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: psf/black
        with:
          options: "--check --verbose"
          src: "../../vectordb-recipes"
          jupyter: true